// -- Component Imports --
import { WorkspaceGate } from "./components/WorkspaceGate";

/** The app root: hands the whole surface to the workspace gate. */
function App() {
  return <WorkspaceGate />;
}

export default App;
